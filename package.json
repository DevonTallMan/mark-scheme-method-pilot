{
  "name": "mark-scheme-method",
  "version": "1.0.0",
  "description": "MSM JS unit test suite",
  "scripts": {
    "test": "jest --testPathPattern=tests/unit/teacher-logic.test.js",
    "test:coverage": "jest --coverage --testPathPattern=tests/unit/teacher-logic.test.js"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
